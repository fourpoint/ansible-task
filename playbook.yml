---
- hosts: all
  become: yes
  pre_tasks:
    - name: Update & Upgrade APT repository
      apt:
        update_cache: yes
        upgrade: dist
  
  roles:
    - firewall
    - githubixx.ansible_role_wireguard
  