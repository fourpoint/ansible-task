---
- name: Install Nginx
  apt: name=nginx state=present

- name: Copy main config file
  template:
    src: nginx.conf.j2
    dest: "/etc/nginx/nginx.conf"
    mode: 0644
    validate: "nginx -t"
  notify: reload nginx