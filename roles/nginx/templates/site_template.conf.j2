server {
        listen 80;

        root {{ site_path }};
        index index.html index.htm;

        server_name {{ site_name }};

        location / {
                autoindex on;
                autoindex_exact_size off;
                autoindex_format html;
        }
}